# Ollama Cloud API key (free tier).
# Sign up and get a key at: https://docs.ollama.com/cloud#cloud-api-access
# When set, the pipeline uses cloud models by default (no local GPU needed
# for fixer and review). Use --local to override.
OLLAMA_API_KEY="your_api_key_here"
